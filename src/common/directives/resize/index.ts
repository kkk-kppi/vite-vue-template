import type { DirectiveBinding, Directive } from 'vue'

const vResize: Directive = {
  created(_el: HTMLElement, _binding: DirectiveBinding) {},

  mounted(_el: HTMLElement, _binding: DirectiveBinding) {}
}

export default vResize
